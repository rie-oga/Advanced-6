/*いいね機能の非同期通信のためのファイル*/

/*js.erbファイルはその名前の通り、javascriptのファイルでありながら、ERBタグを使うことでrubyのコードを書ける優れものです。
コントローラーで定義したインスタンスを<% %>、<%= %>を使うことでそのままjsファイルに記述できます。*/

$("#favorite-<%= @book.id %>").html("<%= j(render 'books/favorite', book: @book) %>");